# Build artifacts
/target
/target/
/target-codex/
/target*/

# Backup files from `cargo fix` / rustfmt
**/*.rs.bk

# Stray local logs
err.log
*.log

# CLI run artifacts (profiler dumps, bytecode summaries, time traces)
profile.out
bytecode-summary.json
trace.json

# Editor / OS cruft
.DS_Store
.idea/

# NOTE: Cargo.lock is intentionally committed (this workspace ships binaries).

# Core dumps (from crashing tests during cross-platform debugging)
core
**/core
*.core

# Launch / marketing prep notes — kept local, never in the public repo
posts/
post.md
research/*.md
fuzz/corpus-cmin/
fuzz/.afl-showmap-temp-*
